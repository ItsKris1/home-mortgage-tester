### Home Mortgage Calculator Tester

Couple tests made with Jest for testing a home mortgage calculator application

### Running the tests

You need to have Node.js installed.

You can run the tests with `npm test`
